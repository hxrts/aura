name: CI / Deep Verify
run-name: ci/deep-verify/${{ github.event_name }}

on:
  push:
    branches: [main, develop]
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  AURA_SUPPRESS_NIX_WELCOME: 1
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lean:
    name: Lean Proofs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-nix
        with:
          cache-name: lean
          enable-store-cache: false
      - name: Build proofs
        run: nix develop --command just ci-lean-build
      - name: Check for incomplete proofs
        run: nix develop --command just ci-lean-check-sorry
      - name: Failure bundle
        if: failure()
        uses: ./.github/actions/failure-bundle
        with:
          name: verify-deep
          replay-command: nix develop --command 'just ci-lean-build && just ci-lean-check-sorry'

  telltale-bridge:
    name: Telltale Bridge
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-nix
        with:
          cache-name: telltale-bridge
          enable-store-cache: false
      - name: Bridge cross-validation
        run: nix develop .#ci --command just ci-telltale-bridge
      - name: Upload bridge artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: telltale-bridge-${{ github.run_id }}
          path: artifacts/telltale-bridge
          if-no-files-found: warn
      - name: Failure bundle
        if: failure()
        uses: ./.github/actions/failure-bundle
        with:
          name: verify-deep
          replay-command: nix develop .#ci --command just ci-telltale-bridge

  kani:
    name: Kani Proofs
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-nix
        with:
          cache-name: kani
          enable-store-cache: false
      - name: Run Kani proofs
        run: nix develop .#nightly --command just ci-kani
      - name: Failure bundle
        if: failure()
        uses: ./.github/actions/failure-bundle
        with:
          name: verify-deep
          replay-command: nix develop .#nightly --command just ci-kani
