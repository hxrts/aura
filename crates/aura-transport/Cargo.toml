# Clean Layer 2 Transport Types and Protocol Definitions
[package]
name = "aura-transport"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
description = "Privacy-aware transport data types and protocol definitions (Layer 2)"
keywords = ["transport", "privacy", "networking", "protocols"]
categories = ["network-programming"]

[lib]
name = "aura_transport"
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
# Core foundation (Layer 1)
aura-core = { path = "../aura-core" }

# Layer 2 dependencies
aura-wot = { path = "../aura-wot" }
aura-journal = { path = "../aura-journal" }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
serde_cbor = { workspace = true }
bincode = { workspace = true }

# Utilities
uuid = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
# Testing only - no runtime dependencies
proptest = { workspace = true }
