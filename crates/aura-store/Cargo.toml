# Domain-focused storage types and logic
[package]
name = "aura-store"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
description = "Storage domain types, semantics, and pure logic for Aura"

[lints]
workspace = true

[dependencies]
# Foundation layer only
aura-core = { path = "../aura-core" }
aura-macros = { path = "../aura-macros" }

# Authorization logic moved to aura-authorization (proper domain separation)

# Serialization
serde = { workspace = true }

# Utilities
hex = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["rt", "macros"] }
async-trait = { workspace = true }
uuid = { workspace = true }
aura-testkit = { path = "../aura-testkit" }

[features]
default = []
