# Aura Social - Social Topology Layer
[package]
name = "aura-social"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Social topology (blocks and neighborhoods) for the Aura threshold identity platform"
keywords = ["social", "topology", "blocks", "neighborhoods", "relay"]
categories = ["cryptography"]

[lints]
workspace = true

[dependencies]
# Layer 1 - Foundation
aura-core = { path = "../aura-core" }

# Layer 2 - Domain crates (aura-social builds ON these)
aura-journal = { path = "../aura-journal" }

# External
async-trait = { workspace = true }
bincode = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
async-trait = { workspace = true }

[features]
default = []
