[package]
name = "aura-storage"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
description = "Capability-based storage layer with choreographic search protocols"

[dependencies]
# Core Aura crates
aura-core = { path = "../aura-core" }
aura-wot = { path = "../aura-wot" }
aura-crypto = { path = "../aura-crypto" }
aura-protocol = { path = "../aura-protocol" }
aura-macros = { path = "../aura-macros" }
aura-mpst = { path = "../aura-mpst" }

# Choreography dependencies (required by aura-macros)
rumpsteak-aura-choreography = { workspace = true }

# Standard dependencies
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
uuid = { workspace = true }
indexmap = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
# tokio-test = { workspace = true } # removed - unused

[features]
default = []
testing = []
